// 도메인: 캠퍼스 자체에 대한 상수.
// 제휴 매장·교내시설 어느 한쪽의 속성이 아니라 학교 위치라는 공통 지식이라 따로 둔다.

/**
 * 에리카 정문 좌표.
 *
 * 용도는 하나뿐 — "사용자가 지금 학교 근처인가"를 재는 **기준점**이다.
 * 지도의 첫 화면 위치가 아니다. 그건 PartnershipMapView의 INITIAL_CENTER가 따로 들고 있고,
 * 정문이 아니라 제휴 매장이 밀집한 상권 한가운데를 가리킨다.
 * (이 값을 고치면 첫 화면이 아니라 아래 반경 판정이 움직인다)
 */
export const ERICA_MAIN_GATE = { lat: 37.2983, lng: 126.8388 } as const;

/**
 * 정문에서 이 거리 밖이면 '학교 근처가 아님'으로 본다.
 * '내 위치' 버튼을 눌렀을 때 집처럼 먼 곳으로 지도가 날아가지 않게 막는다.
 * 정문 좌표와 항상 짝으로 쓰이므로 같은 자리에 둔다.
 */
export const ERICA_NEARBY_RADIUS_M = 2000;
